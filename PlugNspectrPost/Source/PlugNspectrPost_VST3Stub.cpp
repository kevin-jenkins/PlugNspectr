/*
  ==============================================================================
    PlugNspectrPost_VST3Stub.cpp

    Stub translation unit — no functional code.
    Compiled with /MD or /MDd, this causes MSVC to embed a /DEFAULTLIB
    directive in the resulting .obj so the linker pulls in the CRT startup
    object and resolves _DllMainCRTStartup.  Without at least one compiled
    .cpp in the VST3 DLL project the linker cannot find that symbol.

    Do NOT define DllMain here; JUCE's VST3 wrapper defines it inside
    include_juce_audio_processors.cpp (SharedCode).
  ==============================================================================
*/

// Nothing to declare — presence of this TU is the entire fix.
